Put your resume PDF in this folder as:

  Harshil_Jadawala_Resume.pdf

Then open src/data/config.ts and set:

  resume: { available: true, ... }

Every "Download Resume" button across the site will activate automatically.
Until then, the site shows an email-request button instead (never a dead link).
